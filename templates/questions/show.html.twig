{% extends 'base.html.twig' %}

{% block title %}IQuestions - Detail question{% endblock %}

{% block body %}
<div class="container">
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Wording</th>
                <td>{{ question.Wording }}</td>
            </tr>
            <tr>
                <th>Réponses</th>
                <td> |
                {% for choix in question.Answers %}
                    {{ choix }} |
                {% endfor %}
                </td>
            </tr>
            <tr>
                <th>Reponse Correcte</th>
                <td>{{ question.CorrectAnswer }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-3" href="{{ path('app_questions_edit', {'id': question.id}) }}">Modifier la question</a>
    <a class="btn btn-outline-primary mb-3" href="{{ path('app_quizz_show', {'id': question.quizz.id}) }}">Retour au quizz</a>

    {{ include('questions/_delete_form.html.twig') }}
</div>
{% endblock %}
